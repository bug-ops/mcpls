/// A poorly-formatted function for format_document testing.
pub fn poorly_formatted(x: i32, y: i32) -> i32 {
    x + y
}

/// A poorly-formatted struct.
pub struct BadlyFormatted {
    pub a: i32,
    pub b: i32,
}
